import React from 'react';
import ReactDOM from 'react-dom/client';
import ImageBeautifier from './ImageBeautifier.jsx';

ReactDOM.createRoot(document.getElementById('root')).render(
    <React.StrictMode>
        <ImageBeautifier />
    </React.StrictMode>
);
